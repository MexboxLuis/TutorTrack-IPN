package com.example.pitapp.ui.features.auth.components

import com.example.pitapp.R

enum class UserRole(val displayNameResId: Int) {
    TEACHER(R.string.role_teacher),
    STUDENT(R.string.role_student)
}